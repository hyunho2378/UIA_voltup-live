node_modules
dist
.tmp
.env
.env.local
.env.*.local
.DS_Store
.vercel
client/loadtest/reports/
liquidGL-main/
liquidglass-main/
LiquidGlassCheatsheet-main/

# 참고용 원본 아카이브. 바로 옆에 압축 푼 폴더가 있어 중복이다.
*.zip

# 검증 산출물(스크린샷·리포트). 로컬 참고용이라 저장소에 넣지 않는다.
client/test-artifacts/
