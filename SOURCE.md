# SOURCE.md

행사에 실제로 띄우는 문안의 원문. **여기 적힌 텍스트는 문자 그대로 시드한다. 요약·윤문·표현 교체 금지.**
문안을 고치려면 이 파일을 먼저 고치고, 그다음 `supabase/seed.sql` 과 운영 DB 를 맞춘다.

줄바꿈을 강제하고 싶으면 원문 안에 ` / ` 마커를 넣는다(`lib/ko-break.js` 가 그 자리에서 줄을 나눈다).
현재 Q1 은 마커 없이 자동 줄바꿈에 맡긴다.

## 질문 1 (객관식)

의도: 미래 대학이 학생에게 제공해야 할 역량을 묻는다. 청중 다수가 채용·현업 실무자이므로,
"뽑을 때 아쉬웠던 역량"을 떠올려 고르게 하는 것이 목적이다.

**질문**

```
미래의 대학이 학생에게 길러줘야 할 역량은?
```

**선택지** (순서 고정. 대형화면에서 A~D 키로 표시된다)

| 순서 | 라벨 |
|---|---|
| 1 (A) | 문제 정의력 |
| 2 (B) | 데이터 활용력 |
| 3 (C) | 협업과 소통 |
| 4 (D) | 현장 실무 경험 |

선택지를 8자 이내로 맞춘 이유: 대형화면 차트의 라벨 열이 `max-content` 라 라벨이 길어지면
막대가 그만큼 짧아진다(DESIGN.md 차트 절). 20m 거리에서 막대 길이 비교가 먼저 읽혀야 한다.

## 질문 2, 3

아직 확정 전이다. 현재 DB 에는 `[더미]` 접두사가 붙은 임시 문안이 들어 있다.
확정되면 이 파일에 원문을 추가하고 같은 방식으로 시드한다.
