# PATTERNS.md

## 실시간 동기화 패턴 (이 앱의 핵심)

- 세 화면은 Supabase Realtime 채널을 구독한다. 폴링 금지.
- 구독 대상: `session`(활성 질문·투표 상태·공개 여부), `votes`(집계).
- 집계는 클라이언트가 원시 votes를 다 받아 계산하지 않는다. 서버(또는 Supabase view/RPC)가 질문별 집계를 내려주고 화면은 그린다. 100명이 붙어도 각 화면이 전체 원시 데이터를 받지 않게 한다.
- 낙관적 업데이트 지양. 투표 제출은 서버 확정 후 완료 처리. 중복 제출은 서버에서 막는다(브라우저 식별자 + 질문 id 유니크).

## 연결 상태 패턴

- 모든 화면 상단에 ConnectionBadge. connected / reconnecting / disconnected.
- 끊기면 조용히 실패 금지. 대형화면은 마지막 결과 유지 + "재연결 중" 배지. 청중은 재시도 안내.
- 자동 재구독. 재연결 시 현재 session 상태를 다시 fetch해 정합성 회복.

## 투표 카드/리스트 패턴

- 청중 선택지: 세로 스택. 한 번에 하나 선택(단일) 또는 다중은 질문 설정에 따름. 제출 전까지 변경 가능, 제출 후 잠금.
- 제출 버튼은 하단 고정(sticky). 선택 전 비활성.

## 모달/바텀시트 패턴

- 청중 화면 모달은 바텀시트. 어드민 확인 다이얼로그는 중앙 모달.
- 파괴적 동작(투표 초기화 등)은 반드시 확인 다이얼로그.

## 폼 패턴

- 네이티브 select/date/기본 input UI 노출 금지. 전부 커스텀.
- 주관식 입력: 실시간 글자 수, 최대 길이 제한, 공백만 제출 차단.
- 어드민 패스코드: 입력 오류 인라인 표시.

## 빈 상태 패턴

- 투표 대기: EmptyState 일러스트 + "곧 시작합니다".
- 응답 0: 대형화면은 "첫 응답을 기다리는 중".
- 세션 종료: "오늘 세션이 종료되었습니다. 참여해주셔서 감사합니다".

## 에러 패턴

- 인라인 우선. 투표 제출 실패 → 버튼 아래 인라인 + 재시도.
- 치명 오류(세션 없음) → 전체 화면 안내 + 새로고침 유도.
- 네트워크 장애 → 대형화면 배지, 청중 재시도, 어드민 경고.

## 로딩 패턴

- 초기 진입 스켈레톤 최소화. 실시간 앱이라 "연결 중" 상태를 명확히.
- 결과 막대는 값 도착 시 scaleX 애니메이션으로 자연스럽게 성장.

## 행사 당일 장애 대비 패턴 (계획서 명시 사항)

- 짧은 대체 주소(폰에서 직접 타이핑 가능한 short URL) 준비.
- 인터넷 장애 시 오프라인 안내 화면.
- 결과 화면 강제 새로고침 단축키(어드민만).
- 백업 도구(Slido/Mentimeter) 전환 안내를 어드민이 띄울 수 있는 정적 안내 페이지.
