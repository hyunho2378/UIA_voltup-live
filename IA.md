# IA.md

## 화면 목록

세 사용자군이 각기 다른 진입점으로 들어온다. 청중, 대형화면(무대), 진행자(어드민).

### 청중 (폰, QR 진입)
- **`/vote`** 투표 화면. 현재 열린 질문 1개를 보여주고 투표받는다. 어드민이 질문을 바꾸면 자동으로 따라간다.
  - 대기 상태: 투표가 안 열림(마감 포함) → 진행표시 + 질문만. 선택지도 버튼도 문구도 없다.
  - 투표 상태: 선택지 표시, 제출
  - 완료 상태: "투표했어요" 한 줄
  - **마감은 별도 상태가 아니다.** `voting_open=false` 면 대기와 같은 화면이다. 결과는 대형화면에 뜨므로 폰은 조용히 다음 질문을 기다린다.

### 대형화면 (무대 프로젝터)
- **`/screen`** 결과 송출 화면. 현재 질문과 실시간 결과(막대 또는 워드클라우드)를 크게.
  - 대기: 슬로건 + QR + "지금 접속하세요"
  - 투표 중: 질문 + 실시간 집계(공개 여부는 어드민이 제어)
  - 결과 공개: 최종 막대/워드클라우드 강조
  - 전환: 다음 질문으로 넘어가는 트랜지션

### 어드민 (진행자 노트북)
- **`/admin`** 컨트롤 화면. 로그인 후 세션 제어.
  - 질문 리스트와 현재 질문 표시
  - 투표 열기 / 닫기
  - 결과 대형화면 공개 / 숨김
  - 다음 질문 / 이전 질문 이동
  - 실시간 접속자 수, 응답 수 모니터
- **`/admin/login`** 어드민 진입(단순 패스코드).

### 공용
- **`/`** 랜딩. QR로 들어온 게 아니라 도메인만 친 경우 `/vote`로 안내.
- **`/404`, `/500`** 에러.

## 네비게이션 플로우

```
청중:   QR → /vote (질문 자동 동기화, 세션 종료까지 이 화면 유지)
무대:   /screen (행사 내내 고정, 어드민 신호로 상태 전환)
진행자: /admin/login → /admin (질문·투표·공개 제어)
```

세 화면은 서로 이동하지 않는다. 각자 역할 화면에 머무르고, 상태는 Supabase Realtime으로 동기화된다. 어드민의 조작이 청중·대형화면에 즉시 반영되는 단방향 제어 구조.

## 상태 동기화 모델

중심은 `session` 하나. 현재 활성 질문 id, 투표 열림 여부, 결과 공개 여부를 session이 들고 있고 세 화면이 이를 구독한다.

- 어드민이 "질문 2로 이동" → session.active_question 변경 → 청중/대형화면 자동 갱신
- 어드민이 "투표 열기" → session.voting_open = true → 청중 투표 가능
- 어드민이 "결과 공개" → session.results_visible = true → 대형화면 결과 노출
